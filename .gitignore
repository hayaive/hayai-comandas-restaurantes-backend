# dependencias
/node_modules

# build
/dist
/build
# `incremental: true` en tsconfig deja esto en la raíz en cada `npm run build`.
*.tsbuildinfo

# prisma client generado
/src/generated

# imágenes subidas por usuarios (ver CONTRACT.md: necesita volumen persistente
# en producción, no vive en el control de versiones)
/uploads

# entorno
.env
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# editor / os
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# coverage
/coverage

# temporales
*.tmp
